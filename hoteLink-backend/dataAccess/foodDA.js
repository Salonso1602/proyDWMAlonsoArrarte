const Food = require('../entities/dish');

module.exports = {
    getAllFoods : async (queryParams) => {
        //retorna un array con las Dish, el hotelId esta en los params con ese nombre. Puede ser vacío
    },

}